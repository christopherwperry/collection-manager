<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Book Entry</title>
    <link rel="stylesheet" href="https://cdn.shoelace.style/1.0.0-beta20/shoelace.css">
    <link rel="stylesheet" type="text/css" href="style.css"/>
  </head>
  <body>
    {{#book}}
    <div class="book-container">
      <h1>{{title}}</h1>
      <h2>Author: {{author}}</h2>
      {{#genre}}
      <h3>Genre: {{genre}}</h3>
      {{/genre}}
      {{#pages}}
      <h3>Pages: {{pages}}</h3>
      {{/pages}}
      {{#year}}
      <h3>Published: {{year}}</h3>
      {{/year}}
      {{#isbn}}
      <h3>ISBN: {{isbn}}</h3>
      {{/isbn}}
      {{#language}}
      <h3>Language: {{language}}</h3>
      {{/language}}
      <h3>Editions</h3>
      {{^editions}}
      <p>
        No editions entered.
      </p>
      {{/editions}}
      <ul>
        {{#editions}}
        <li>
          Published in: {{year}} by {{publisher}} in {{location}}.
        </li>
        {{/editions}}
        </ul>
        <p>
          <a href="/{{title}}/new_edition/" class="button">Add an Edition</a>
        </p>
    </div>
    <p>
      <a class="button" href="/{{title}}/edit/">Edit Book Entry</a>
    </p>

    <form class="delete" action="/{{title}}/delete" method="post">
      <button type="submit" name="button">Delete Book Entry</button>
    </form>

    {{/book}}
    <br>
    <p>
      <a href="/" class="button">Back to Library</a>
    </p>
    <script src="https://cdn.shoelace.style/1.0.0-beta20/shoelace.js"></script>
  </body>
</html>
